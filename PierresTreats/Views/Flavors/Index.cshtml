@{
  Layout = "_Layout";
}
@using PierresTreats.Models

<h2>Pierre's Delectable Flavors:</h2>
<hr />

@if(Model.Count == 0)
{
  <p>Pierre currently has no flavors listed.</p>
}
<ul>
  @foreach(Flavor flavor in Model)
  {
    <li>@Html.ActionLink($"{flavor.Name}", "Details", new { id = flavor.FlavorId })</li>
  }
</ul>
